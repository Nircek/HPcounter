<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:tools="http://schemas.android.com/tools"
      tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
                android:id="@+id/nav_counter_1"
                android:icon="@android:drawable/ic_lock_idle_alarm"
                android:title="Counter 1"/>
    </group>

    <item android:title="@string/menu_add_counter" android:icon="@android:drawable/ic_input_add"
          android:id="@+id/nav_add_counter"/>
    <group
            android:id="@+id/sett_group">
        <item android:title="@string/action_settings" android:id="@+id/nav_sett"
              android:icon="@drawable/ic_menu_manage"/>
    </group>

</menu>
